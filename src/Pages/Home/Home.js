import { Grid } from "@mui/material";
import React, { useEffect } from "react";

const Home = () => {
  useEffect(() => {
  }, []);
  return <Grid  item xs={10}>Home</Grid>
};

export default Home;
